package com.immo.gestion.bien.domain;

public enum ModaliteCharges {
    /** Forfait non régularisable — logement meublé (loi 1989 art. 25-10). */
    FORFAIT,
    /** Provision sur charges avec régularisation annuelle — logement nu (loi 1989 art. 23). */
    PROVISION
}
